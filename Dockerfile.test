FROM golang:1.24-alpine

ENV GOTOOLCHAIN=auto
RUN apk add --no-cache git docker-cli

WORKDIR /src

COPY go.mod go.sum ./
RUN go mod download

COPY . .

CMD ["sh", "-c", "go test -v ./... && go test -tags=integration -v -timeout=300s ./tests/"]
